# Assign a dictionary to variable day_temperatures. The dictionary should contain three keys: 'morning', 'noon', and
# 'evening' and each key should contain a tuple as a value. Each tuple should contain three floats.

day_temperatures = {'morning': (7.2, 8.4, 4.2), 'noon': (10.2, 18.4, 14.2), 'evening': (8.2, 2.4, 3.2)}
